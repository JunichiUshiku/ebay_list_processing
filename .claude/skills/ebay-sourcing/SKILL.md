---
name: ebay-sourcing
description: eBay販売商品の国内仕入れ先検索ワークフロー。スプレッドシートのeBay商品リストから、国内11サイト（Amazon、メルカリ、ヤフオク等）で同一商品を検索し、希望価格・コンディション条件に合う仕入れ先を見つけてスプレッドシートに記録する。「仕入れ先を探して」「ebay sourcing」「在庫仕入れ」「国内サイトで検索」などのキーワードで使用。
---

# eBay在庫仕入れ処理

## 🚨 CRITICAL RULES (MUST PRESERVE AFTER COMPACTION)

**REQUIRED**: Read `references/sites.md` FIRST before any processing
**REQUIRED**: Check all 11 domestic sites for sourcing
**REQUIRED**: Verify product matches eBay listing (image/title comparison)
**REQUIRED**: Apply price and condition filters strictly

**必須**: 処理開始前に `references/sites.md` を必ず読み込む
**必須**: 11サイト全てを検索対象とする
**必須**: eBay商品と同一品であることを画像・タイトルで確認
**必須**: 価格条件・コンディション条件を厳守

---

## 🔴 必須参照ファイル（最初に読み込むこと）

**以下のファイルをReadツールで読み込んでから処理を開始すること。**

| ファイル | パス | 内容 |
|----------|------|------|
| サイト一覧 | `.claude/skills/ebay-sourcing/references/sites.md` | 国内11サイトの検索URL |

```
Read: .claude/skills/ebay-sourcing/references/sites.md
```

**⚠️ このファイルを読み込まずに処理を開始することは禁止。**

---

## スプレッドシート定義

**Spreadsheet ID**: `1pmbzGCHCqd0EiyuJBl6rfUEGXVITcBDMGPg9bQ67d-g`
**Sheet名**: `AI作業用`

| 列 | 内容 | 備考 |
|----|------|------|
| A | 店舗名/ID | 検索対象の識別子 |
| B | 商品名 | 検索キーワードとして使用 |
| C | eBay Item Number | eBayページ確認用 |
| D | 仕入URL | 既存の仕入れ先URL |
| F | 仕入価格（円） | 空/0→スキップ |
| P | メモ（コンディション） | 新品/中古 |
| U | 結果URL | 値あり→スキップ（処理済み） |
| V | その他候補URL | 最大5件、スラッシュ区切り |
| W | 備考 | 価格、状態、エラー等 |

ヘッダー: 1行目、データ: 2行目から

---

## ワークフロー概要

1. TodoWriteでタスク登録（必須・最初に実行）
2. スキル状態記録
3. 商品情報取得
4. eBayページ確認
5. 検索キーワード確定
6. 国内11サイト一括検索
7. 仕入れ判断
8. 結果記録
9. 後処理
10. 完了報告

---

## ワークフロー詳細

### Step 1: TodoWriteでタスク登録（必須・最初に実行）

**⚠️ このステップを最初に実行すること。スキップ禁止。**

TodoWriteツールで以下のタスクを登録し、進捗を可視化:

```
TodoWrite:
- 参照ファイル（sites.md）を読み込む (pending) ← 最初に実行
- スキル状態を記録 (pending)
- スプレッドシートからデータ取得 (pending)
- 処理対象行の特定 (pending)
- eBayページで商品情報確認 (pending)
- 国内11サイトで仕入れ先検索 (pending)
- 結果をスプレッドシートに記録 (pending)
- サマリー報告を出力 (pending)
```

**重要**: 各ステップ完了時に `status: completed` に更新すること。

### Step 1.5: 参照ファイル読み込み（必須）

**TodoWrite更新**: `参照ファイル（sites.md）を読み込む` → `in_progress`

```
Read: .claude/skills/ebay-sourcing/references/sites.md
```

このファイルには国内11サイトの検索URLテンプレートが記載されている。
Step 6で使用するため、必ず読み込んでおくこと。

**TodoWrite更新**: `参照ファイル（sites.md）を読み込む` → `completed`

### Step 2: スキル状態記録

**TodoWrite更新**: `スキル状態を記録` → `in_progress`

```bash
cat > ~/.claude/skill-state.json << 'EOF'
{
  "skill": "ebay-sourcing",
  "message": "スキル ebay-sourcing を再実行し、未処理の行から仕入れ先検索を再開してください。"
}
EOF
```

**TodoWrite更新**: `スキル状態を記録` → `completed`

### Step 3: 商品情報取得（MCP経由）

**TodoWrite更新**: `スプレッドシートからデータ取得` → `in_progress`

```
mcp__google-sheets__get_sheet_data
spreadsheet_id: 1pmbzGCHCqd0EiyuJBl6rfUEGXVITcBDMGPg9bQ67d-g
sheet: AI作業用
range: A2:W
```

**スキップ判定**（U列の値を確認）:
- URL形式（`https://`）or「なし」or「スキップ」→ 処理済み、次の行へ
- 数値のみ（「0」「1」等）or 空 → **未処理、検索実行**

**F列が空/0** → U列に「スキップ」記載して次の行へ

**TodoWrite更新**:
- `スプレッドシートからデータ取得` → `completed`
- `処理対象行の特定` → `in_progress` → `completed`

### Step 4: eBayページ確認

**TodoWrite更新**: `eBayページで商品情報確認` → `in_progress`

- URL: `https://www.ebay.com/itm/{C列}`
- 商品画像・タイトルを確認（同一商品判断用）
- ページ存在しない → W列に記載してスキップ
- ENDED（売り切れ）でも仕入れ先は探す

### Step 5: 検索キーワード確定

- B列に値あり → そのまま使用
- B列が空 → eBayタイトル・画像から日本語キーワードを推測

### Step 6: 国内11サイト一括検索

**TodoWrite更新**: `国内11サイトで仕入れ先検索` → `in_progress`

**検索サイト一覧**: Step 1.5で読み込んだ `references/sites.md` の11サイトを使用

⚠️ **sites.md を読み込んでいない場合は、先に読み込むこと**

1. **URL生成**: `##SEARCH##`をキーワードに置換して11サイト分のURLを生成
2. **一括オープン**: 全URLを新しいタブで開く
3. **順次確認**: 各タブを確認し、ヒット数0件なら次のタブへ

**確認ルール**:
- ヒット数0件 → 次サイトへ（タブを閉じる）
- ヒットあり → 商品詳細を確認して仕入れ判断
- 見つからない場合はキーワード調整して再検索可

### Step 7: 仕入れ判断

**同一商品判断**:
- eBay画像・タイトルと照合
- 色違い・サイズ違い → 別商品
- セット商品のバラ売り → 対象外

**価格条件**:
- F列の希望価格以下（税込み・送料込み）
- 送料不明時 → +1300円で計算
- ポイント還元は考慮しない

**オークション判断**:
- 即決商品優先
- 現在価格が希望価格内ならOK（即決あればそちら優先）
- 入札中商品も対象

**コンディション条件**:
| P列 | 判断 |
|-----|------|
| 「中古」or 空 | 新品・中古どちらもOK |
| 「新品」 | 新品優先、なければ中古可（W列に記載） |

| 表記 | 扱い |
|------|------|
| 「未使用に近い」 | 新品扱い |
| 「目立った傷や汚れなし」 | 中古扱い |
| 「難あり」「訳あり」「破損」「欠品」「ジャンク」 | 除外 |
| 「箱なし」「説明書なし」 | 他に候補なければ適用可 |
| 売り切れ商品 | 除外 |

### Step 8: 結果記録（MCP経由）

**TodoWrite更新**: `結果をスプレッドシートに記録` → `in_progress`

```
mcp__google-sheets__update_cells
spreadsheet_id: 1pmbzGCHCqd0EiyuJBl6rfUEGXVITcBDMGPg9bQ67d-g
sheet: AI作業用
range: U{行番号}:W{行番号}
data: [[U列の値, V列の値, W列の値]]
```

| セル | 記録内容 | 例 |
|------|----------|-----|
| **U{行}** | ベストな商品URL or 「なし」 | `https://auctions.yahoo.co.jp/...` |
| **V{行}** | その他候補URL（最大5件、スラッシュ区切り） | `https://... / https://...` |
| **W{行}** | 備考（価格、状態、エラー等） | `¥26,800(税送込)、中古` |

**重要**: U列に既存の数値（「0」「1」等）があっても**上書きする**

### Step 9: 後処理

1. 検索サイトの不要タブを閉じる
2. 次の商品行へ移動してStep 4〜8を繰り返す
3. **10件ごとに進捗報告**

**TodoWrite更新**:
- `国内11サイトで仕入れ先検索` → `completed`
- `結果をスプレッドシートに記録` → `completed`

### Step 10: 完了処理

**TodoWrite更新**: `サマリー報告を出力` → `in_progress`

**サマリー報告（テーブル形式）**:

| 項目 | 結果 |
|------|------|
| 処理件数 | {処理した件数} |
| 仕入れ先発見 | {発見件数} |
| 見つからず | {見つからなかった件数} |
| スキップ | {スキップ件数} |
| エラー | {エラー件数}（行番号: {該当行}） |

**TodoWrite更新**: `サマリー報告を出力` → `completed`

---

## 実行パラメータ

| 指定 | 動作 |
|------|------|
| 「全て」 | 上から順に全件処理（U列に値ある行はスキップ） |
| アイテムナンバー指定 | 指定行のみ処理 |
| 再処理 | U列を消して再実行 |

---

## 前提条件

- Google Sheets MCPサーバー設定済み
- サービスアカウントがスプレッドシートに共有されている（編集権限）
- Cookie同意ダイアログは「許可」で対応（国内サイト検索時）
