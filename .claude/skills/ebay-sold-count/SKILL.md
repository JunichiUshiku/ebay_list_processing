---
name: ebay-sold-count
description: eBay販売履歴件数調査ワークフロー。スプレッドシートのE列eBay検索URLからキーワードを抽出し、eBay Seller Hub Product Researchで90日間の販売件数（Total Sold合計）を調べてX列に記録する。90日間の販売件数が2未満の場合は6ヶ月間で再検索しY列に記録。「販売件数を調べて」「sold count」「90日間の販売数」「リサーチ」で全件処理。アイテムナンバーを指定した場合（例:「403498476787の販売数を調べて」）はC列から該当行を検索して処理。
---

# eBay販売件数調査処理

## 🚨 CRITICAL RULES (MUST PRESERVE AFTER COMPACTION)

**REQUIRED**: Read `references/selectors.md` FIRST before any processing
**FORBIDDEN**: dayRange URL parameter changes
**REQUIRED**: UI pulldown → Research button click

**必須**: 処理開始前に `references/selectors.md` を必ず読み込む
**絶対禁止**: URLパラメータ（dayRange）での期間変更
**必須手順**: 期間変更は必ずUIプルダウン → Researchボタンクリック

---

## 🔴 必須参照ファイル（最初に読み込むこと）

**以下のファイルをReadツールで読み込んでから処理を開始すること。**

| ファイル | パス | 内容 |
|----------|------|------|
| セレクター | `.claude/skills/ebay-sold-count/references/selectors.md` | DOM要素セレクターとJavaScriptコード |

```
Read: .claude/skills/ebay-sold-count/references/selectors.md
```

**⚠️ このファイルを読み込まずに処理を開始することは禁止。**

---

## スプレッドシート定義

**Spreadsheet ID**: `1pmbzGCHCqd0EiyuJBl6rfUEGXVITcBDMGPg9bQ67d-g`
**Sheet名**: `AI作業用`

| 列 | 内容 | 備考 |
|----|------|------|
| C | eBay Item Number | アイテムナンバー指定時の検索対象 |
| E | eBay検索URL | `_nkw=`パラメータからキーワード抽出 |
| X | 販売数（90日間） | 処理結果を記録（値あり→スキップ） |
| Y | 販売数（6ヶ月間） | X列が2未満の場合のみ記録 |

ヘッダー: 1行目、データ: 2行目から

---

## ワークフロー概要

1. TodoWriteでタスク登録（必須・最初に実行）
2. スキル状態記録
3. ヘッダー確認
4. データ取得
5. 処理対象決定
6. キーワード抽出
7. 検索実行
8. 販売数取得
9. 結果記録
10. ループ処理
11. 完了報告

---

## ワークフロー詳細

### Step 1: TodoWriteでタスク登録（必須・最初に実行）

**⚠️ このステップを最初に実行すること。スキップ禁止。**

TodoWriteツールで以下のタスクを登録し、進捗を可視化:

```
TodoWrite:
- 参照ファイル（selectors.md）を読み込む (pending) ← 最初に実行
- スキル状態を記録 (pending)
- X1/Y1のヘッダー確認・記載 (pending)
- 処理対象行の特定 (pending)
- 各アイテムの販売数調査 (pending)
- 結果をスプレッドシートに記録 (pending)
- サマリー報告を出力 (pending)
```

**重要**: 各ステップ完了時に `status: completed` に更新すること。

### Step 1.5: 参照ファイル読み込み（必須）

**TodoWrite更新**: `参照ファイル（selectors.md）を読み込む` → `in_progress`

```
Read: .claude/skills/ebay-sold-count/references/selectors.md
```

このファイルには以下が記載されている:
- DOM要素のセレクター一覧
- キーワード入力、期間選択、Researchボタンクリック等のJavaScriptコード
- Total Sold取得コード

Step 7, 8で使用するため、必ず読み込んでおくこと。

**TodoWrite更新**: `参照ファイル（selectors.md）を読み込む` → `completed`

### Step 2: スキル状態記録

**TodoWrite更新**: `スキル状態を記録` → `in_progress`

```bash
cat > ~/.claude/skill-state.json << 'EOF'
{
  "skill": "ebay-sold-count",
  "message": "スキル ebay-sold-count を再実行し、X列が空の行から処理を再開してください。UIプルダウンで期間を選択し、Researchボタンをクリックして検索を実行すること。URLパラメータでの期間変更は禁止。"
}
EOF
```

**TodoWrite更新**: `スキル状態を記録` → `completed`

### Step 3: X列・Y列ヘッダー確認

**TodoWrite更新**: `X1/Y1のヘッダー確認・記載` → `in_progress`

1. X1・Y1セルを取得:
```
mcp__google-sheets__get_sheet_data
spreadsheet_id: 1pmbzGCHCqd0EiyuJBl6rfUEGXVITcBDMGPg9bQ67d-g
sheet: AI作業用
range: X1:Y1
```

2. ヘッダーが空の場合 → 書き込み:
```
mcp__google-sheets__update_cells
range: X1:Y1
data: [["販売数（90日間）", "販売数（6ヶ月間）"]]
```

**TodoWrite更新**: `X1/Y1のヘッダー確認・記載` → `completed`

### Step 4: 必要な列のみデータ取得

**重要**: C1:Xのような広範囲取得は禁止（トークン超過の原因）

4列を個別に取得（並列実行可）:
- C列（アイテムナンバー）: `range: C:C`
- E列（eBay URL）: `range: E:E`
- X列（90日間販売件数）: `range: X:X`
- Y列（6ヶ月間販売件数）: `range: Y:Y`

### Step 5: 処理対象の決定

**TodoWrite更新**: `処理対象行の特定` → `in_progress`

**アイテムナンバー指定あり**:
- C列から検索し**行番号を特定**
- 特定した行番号と同じ行のE列からURL取得
- X列の値に関係なく処理

**指定なし（全件処理）**:
- X列が空 → 未処理、検索実行対象
- X列に数値あり → 処理済み、スキップ

**TodoWrite更新**: `処理対象行の特定` → `completed`

### Step 6: キーワード抽出

**TodoWrite更新**: `各アイテムの販売数調査` → `in_progress`

E列URLから`_nkw=`パラメータを抽出:

```
入力: https://www.ebay.com/sch/i.html?_nkw=Crab+Plate&_sacat=0
抽出: Crab Plate（+を空白に変換）
```

**エラーケース**: `_nkw=`が存在しない → X列に「URLエラー」記録

### Step 7: リサーチページで検索実行

**トップページURL**: `https://www.ebay.com/sh/research?marketplace=EBAY-US&tabName=SOLD`

**手順**（Step 1.5で読み込んだ `references/selectors.md` のセレクター/コードを使用）:

⚠️ **selectors.md を読み込んでいない場合は、先に読み込むこと**

1. リサーチトップページにナビゲート（初回のみ）
2. キーワード入力（javascript_tool）
3. 期間プルダウンをクリックしてメニュー展開
4. 「Last 90 days」を選択
5. Researchボタンをクリック

### Step 8: 販売件数取得

#### 8-1: 90日間の販売件数取得

javascript_toolで以下を取得（Step 1.5で読み込んだ `selectors.md` のコードを使用）:

1. **Total Sold合計を取得**
2. **現在のURL（window.location.href）を取得**

**取得値** → 販売数とURLをペアで保持し、Step 9でX列に記録

#### 8-2: 6ヶ月間の追加検索（条件付き）

**条件**: 90日間のTotal Soldが**2未満**の場合のみ実行

1. 期間プルダウンをクリック
2. 「Last 6 months」を選択
3. Researchボタンをクリック
4. **Total Sold合計を取得**
5. **現在のURL（window.location.href）を取得**

**取得値** → 販売数とURLをペアで保持し、Step 9でY列に記録（0でも記録）

**条件分岐まとめ**:
| 90日間の結果 | 6ヶ月検索 | X列 | Y列 |
|--------------|-----------|-----|-----|
| 2以上 | 実行しない | 90日間の値（リンク付き） | 空 |
| 2未満（0, 1） | 実行する | 90日間の値（リンク付き） | 6ヶ月間の値（リンク付き） |

### Step 9: 結果記録（バッチ書き込み）

**TodoWrite更新**: `結果をスプレッドシートに記録` → `in_progress`

10件ごとにMCPツールでバッチ書き込み:

#### 書き込み形式: HYPERLINK関数

販売数に検索結果URLへのリンクを埋め込む:

```
mcp__google-sheets__batch_update_cells
sheet: AI作業用
ranges: {
  "X{行番号}": [['=HYPERLINK("{90日間検索URL}", "{90日間販売件数}")']],
  "Y{行番号}": [['=HYPERLINK("{6ヶ月間検索URL}", "{6ヶ月間販売件数}")']]  // 条件該当時のみ
}
```

**例**:
```
"X5": [['=HYPERLINK("https://www.ebay.com/sh/research?marketplace=EBAY-US&keywords=Crab+Plate&dayRange=90&tabName=SOLD", "3")']]
```
→ セルには「3」と表示され、クリックで検索結果ページに遷移

**注意事項**:
- URL内に `"` が含まれる場合は `""` にエスケープする
- エラー時（URLエラー、タイムアウト等）はリンクなしで文字列のみ記録
- 販売数が0でもリンク付きで記録（検索結果確認のため有用）

### Step 10: ループ処理

1. Step 6-9を繰り返し
2. **10件ごとに進捗報告**: 「進捗: 20/250件完了（8%）」
3. **30件ごとにページリロード**（メモリ管理）

**TodoWrite更新**:
- `各アイテムの販売数調査` → `completed`
- `結果をスプレッドシートに記録` → `completed`

### Step 11: 完了処理

**TodoWrite更新**: `サマリー報告を出力` → `in_progress`

**サマリー報告（テーブル形式）**:

| 項目 | 結果 |
|------|------|
| 処理件数 | {処理した件数} |
| 成功 | {成功件数} |
| エラー | {エラー件数}（行番号: {該当行}） |
| 6ヶ月検索実行 | {6ヶ月検索を実行した件数} |

**TodoWrite更新**: `サマリー報告を出力` → `completed`

---

## エラーハンドリング

| エラー種別 | X列記録値 |
|------------|-----------|
| URL形式不正 | 「URLエラー」 |
| ログイン切れ | 処理停止、ユーザー通知 |
| 検索結果なし | 0 |
| ページタイムアウト | 「タイムアウト」 |
| DOM要素未検出 | 「取得エラー」 |

**リトライ**: 最大3回、間隔2秒→5秒→10秒

---

## 実行パラメータ

| 指定 | 動作 |
|------|------|
| 「全て」 | X列が空の全行を処理 |
| 行番号指定 | 指定行以降のX列空の行を処理 |
| 件数指定 | 指定件数のみ処理 |
| アイテムナンバー指定 | C列から該当行を検索して処理 |

---

## 前提条件

- Google Sheets MCPサーバー設定済み
- eBay Seller Hubアクセス権限あり
- Chrome拡張（claude-in-chrome）でブラウザ操作可能
